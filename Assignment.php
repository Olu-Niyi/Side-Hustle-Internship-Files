<?php


//array declaration
    $Usernames = array("Ted", "Barney", "Marshall", "Lily", "Robin");
  
    if strlen($Usernames<6)
  //function to generate 3 random numbers
  function randomNumbers(){
    for ($i=0; $i < 3; $i++) { 
        $random = rand(100, 999);
        return $random;
    }    
    
    if (strlen($username >=8)
    Echo "Username cannot be greater than 8 or less than 6"
//headers for password declaration
    $Passwords = array("abc", "def", "ghi", "jkl", "mno");
    
    echo "$Usernames $passwords";

    ?> 